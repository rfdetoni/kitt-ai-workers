[project]
name = "kitt-ai-workers"
version = "0.1.1"
requires-python = ">=3.10"
dependencies = [
  "kitt-protocol @ git+https://github.com/rfdetoni/kitt-protocol.git@cdcffb1539e484c82bc1368b77315214712df88b#subdirectory=sdk/python",
]

[project.optional-dependencies]
stt = [
  "faster-whisper>=1.0",
]

[project.scripts]
kitt-stt = "kitt_workers.stt_server:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["kitt_workers*"]
exclude = ["packages*"]
